import React from 'react';

const knightStyle = {
	fontSize: 60,
	fontWeight: 'bold',
	cursor: 'move',
};
const Knight = () => {
	return (
		<div style={knightStyle}>
			♘
		</div>
	);
}

export default Knight;
